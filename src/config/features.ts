/** US payins/payouts (ACH, RTP) and Plaid bank linking. */
export const US_RAILS_ENABLED = false;

/** Moving USD (send, own transfers, card settlement). */
export const USD_MOVEMENTS_ENABLED = false;
