/** US payins/payouts (ACH, RTP) and Plaid bank linking. */
export const US_RAILS_ENABLED = false;

/** USD as a primary balance and in movements (send, transfers, cards). */
export const USD_ENABLED = false;

/** Card tab, card details and card creation. */
export const CARDS_ENABLED = false;
